`Table 1` Lake abundance and lake size by landscape type. Requires PF_PLD.csv, which is an output of `data_curation/create_PF_PLD.py` and northern_pf_land_area.csv, which is an output of `Tables/landscape_area.py`

`Table 2` Lake coverage and lake density by landscape type. Requires PF_lake_density_coverage.csv, which is the csv version of PF_lake_density_coverage.gpkg, archived through the Arctic Data Center. Also relies on global_lake_density_fraction.csv, which is an ouptput of `data_curation/global_lake_density_fraction`

`landscape_area` Determine area covered by each landscape class
